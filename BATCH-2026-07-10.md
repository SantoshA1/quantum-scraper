# QTP Batch 2026-07-10 — Edge-Measurement Ledger + Fractional-Kelly Gate

Everything in this batch is live on `qtp_prod` (Supabase `vdmtwmwpxvohodyrdlon`) and
n8n (tradenextgen.app.n8n.cloud) as of 2026-07-10. Paper mode only; live branch dormant
until go-live criteria are met.

## Database (docs/*.sql — applied to qtp_prod)

- `202607091500_qet_edge_ledger.sql` — `trade_ledger` (intent vs execution per trade,
  slippage/P&L/R derived by DB trigger) + `edge_metrics_by_strategy` view (win rate,
  E[R], profit factor, t-stat, annualized Sharpe, full-Kelly f*). RLS on, service-role
  writes only, realtime enabled. Prereq: canonical QET bootstrap (also applied 07-09).
- `202607091510_qet_kelly_gate.sql` — `compute_kelly_gate()` RPC (Gate K):
  probation 0.50% risk below 40 closed trades/90d per strategy, quarter-Kelly capped by
  Gate-1 1%, drawdown de-lever x1.0/x0.6/x0.35, hard halt at 12% DD, fails closed.
  Security-hardened (security_invoker view, pinned search_path).

## n8n workflows (n8n-workflows/*.json — published + active)

- `qet-kelly-risk-gate-h2.json` — signal webhook `/webhook/qet-signal-ingest`,
  all six QET safety gates, Kelly RPC replaces Gate-1 sizing math, Telegram approval on
  live branch (5-min timeout = reject), Alpaca bracket orders, ledger H3 insert,
  audit_log on every path.
- `qet-h4-exit-sync.json` — every 5 min ET: polls open ledger rows, fetches Alpaca
  order + bracket legs, writes exits/busts back; DB trigger derives cost-survived R.
- `qet-h5-reconciliation-weekly-report.json` — nightly 2 AM ET broker-fill
  reconciliation (orphan fills, stale opens); Sunday 8 AM edge scoreboard to Telegram.

## Verification (maya fixture, user e162f509)

- Gate verdicts proven on prod: probation 0.5% | negative_measured_edge
  (kelly_star -0.4823 on seeded W=35.6%, +0.8R/-1.1R) | drawdown_halt at 12.5% DD.
- Ledger derivation trigger proven to the decimal (10.0 bps entry slip, R 0.375) and
  H4's UPDATE proven against prod in a rolled-back transaction (exit slip 6.12 bps,
  net -540, R -1.08).
- Production smoke test: webhook -> reject -> audit row -> Telegram alert delivered.

## Go-live criteria (data deadline, not calendar)

>= 40 closed trades per strategy, E[R] > 0 after measured costs, t-stat >= 2,
H5 reconciliation clean. Then: create Alpaca-LIVE credential, extend H4 SELECT to
live mode, start live at probation sizing. Context: 07-07 cost-truth report measured
-13bp/side slippage — the ledger exists so sizing never again trusts a zero-cost
assumption.

## Open items

GitHub Gate-3 auto-commit node in H2 still needs `GitHub-QET` n8n credential + repo
URL; fee true-up from Alpaca activities; options multi-leg support; H4 live-mode
extension.
