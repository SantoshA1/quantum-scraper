# gov 244 — The exit half of the ratified policy never ran: dead calendar starved the due-list (2026-08-31)

**The desk snapshot's central finding is confirmed and now fixed.** Friday 15:50's
time-exit run found 0 due because the session clock counted trading days from
`quantum.scorer_bars_daily` — whose feed is E1-run-driven, not nightly, and **froze
at 08-24** (7 days dead at RCA). FLEX, at true session 3, read as "session 1."
gov-241's fail-safe #2 ("a stalled feed means exits fire late, never early")
degraded to **never**. Worse: **PIM's I6 used the same dead calendar** — the monitor
shared its clock with the mechanism it was checking, so the missed exit raised no
alarm. Correlated failure, both halves.

## The fix (live 10:2x ET, five hours before today's run)

Deterministic session clock in BOTH consumers: weekday count over
`(entry_date, today]` minus a hardcoded 2026 NYSE holiday list (Labor Day,
Thanksgiving, Christmas — renew each December). No external feed can starve date
arithmetic. Deploys: time-exit `3de7ae90 → 2fecc459`; PIM v1.4 `7643fe3a`. Suites
23/23 and 28/28 with a holiday-drop sabotage biting. Live replay of the fixed
selector returns exactly **FLEX (session 3, overdue), AVGO (2), ARES (2)** — DGX
(legacy, epoch-excluded), SBAC/SNAP (session 1), SNDK (0) correctly out. Today's
15:50 scheduled run executes them; no manual orders were or will be placed.
**Design rule ratcheted into both files: a monitor must never share its clock — or
any input feed — with the mechanism it monitors.**

## Snapshot verification (their claim → ledger truth)

| claim | verdict |
|---|---|
| 30-day net −$3,588 | **−$3,588.15 — exact** |
| 40 stop-outs, 0% win, −$6,521 (90d) | **40 / 0 / −$6,520.92 — exact** |
| manual exits only green bucket +$1,784 | **+$1,784.46 (6 of 12 won) — exact** (trail bucket is also net green: +$396, carried by one LDOS +10.2R) |
| 7-day: 5 closed, 0 wins, 100% stopped, −$901 | **5 / 0 / 5 stopped / −$900.55 — exact** |
| W34 −$1,513 / W35 −$1,204 / W36 −$316 | Directionally right; my price-diff basis reads −$1,163 / −$602 / −$299 — the deltas are fee-inclusive net_pnl vs gross and week-boundary conventions, not a data dispute |
| FLEX still open ~−$137 | open, session 3, exits today 15:50 |
| SNDK "sat UNKNOWN until TSM caught up" / did it get a stop? | **Protected**: nested OCO coverage ≥ qty (PRS 10:15 ET), stop exactly 2.500% of fill, `reanchored:true` = Patch C's EX-C3 firing CORRECTLY on a pathological fill (the designed re-anchor to policy geometry, suite EXD-04's case) |
| "Trails frozen, cannot save the book" | true — gov 241 T2, deliberate |
| "Machinery is not the 2-week story" | half right: entries, stops, sizing all execute clean — but the POLICY was never fully running: its exit half was dead. The 40-stop bucket includes zero trades that ever had a working 2-day scratch exit |

## The blunt read

E1 measured **2.5% stop + 2-day time exit** as PF > 1.2. What actually traded was
**2.5% stop + no time exit** — a policy E1 never endorsed. Every cohort trade so far
(n=3, 0 wins, −$645) could only exit by stop or by hand. The real test of the
ratified policy **starts today at 15:50**, when the scratch exit exists for the
first time. Full agreement with the snapshot's discipline, unchanged from the gov-242
freeze: no new filters, no stop changes, no model swap — any of that now would hide
the test. Judgment stands at n=10 (I10 tripwire) and n=20.

**Live money is not a conversation** — concur without reservation. The standing
gates before it ever becomes one: a cohort of n≥20 with working exits clearing
PF ≥ 1.2, plus the kernel items already on the Conclave menu (event-driven
protection off the fill stream, model off the hot path, dedicated execution
process). Nothing about two red weeks at 0.5% probation sizing changes the plan;
it IS the plan's cost of measurement — but the measurement only became real today.

## Ledger / watch

- Brake: −$645.35 of −$1,250 (51.6%) after this morning's WAT stop (cohort loss #3).
- 15:50 today: FLEX/AVGO/ARES time-exit (first live firings; H4c labels them `time`).
- 18:25 tonight: heartbeat with independent-clock I6; SBAC/SNAP due tomorrow.
- `scorer_bars_daily` remains dead — now irrelevant to trading (dependency removed
  everywhere); flagged for the E-series backlog (analysis needs fresh bars).
- PR #16 still awaits the one push from the PO's terminal.
