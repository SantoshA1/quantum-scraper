# gov 240 — Why no trades (final long-form answer), whether QTP is learning, and the way out

**2026-08-25, 15:45 ET.** Direct answers to all three questions, then the recommendation
that ends the freeze without loosening a single safety gate.

---

## 1 · Why zero orders — verified end to end, again, and for the last time in long form

**218 candidates today, every one accounted for, zero anomalies:** bias-path 49, short-halt
45, Gate-K 25, shadow 25, SSM 21, bias-filter 17, volume 12, AI-conflict 11, MTF 10,
blocklist 3. Zero AUTH_FAILED (day 6), zero EARNINGS_WINDOW (correct — no candidate near a
print), calendar self-refreshed last night (1,810 rows), DMS ran its **first scheduled
morning with the liveness leg** (10:15, green), DGX fully protected at **+$388**.

The machine is not broken. It has not been broken on any of the six days you've asked. The
answer has been the same two sentences since 08-19: **longs are blocked by Gate-K's
measured edge (PF 0.6585 over n=27), shorts by the gov-219 halt.** Everything else in the
funnel is normal filtration.

**This question now costs one query instead of an investigation** (gov 240):

```sql
select verdict from quantum.v_qtp_why_no_trades;
```

Today it returns: *"LONGS locked by Gate-K negative_measured_edge alone (PF 0.6585 over
n=27; regime CHOP not blocking). SHORTS halted (gov 219). Unlock paths: Conclave policy
adoption (E1), D2 quarantine + green close, or window roll 2026-10-18."* Point the
scheduled morning check at this view and the daily token burn on this question drops to
almost nothing.

## 2 · Is the self-loop working? Yes — and here is the split that matters

**The SHADOW loop is learning, demonstrably.** Ran E1's third out-of-sample pass today
(`e1_20260825`, execution 646969). The blocked-BUY cohort has grown **324 → 395 → 490**
purely from candidates the gates kept rejecting — the system studies every trade it
refuses. The verdict across three runs of the 2-day-exit family (B cohort):

| stop × time_2d | 08-19 | 08-21 | 08-25 | trajectory |
|---|---|---|---|---|
| 1.0% | 1.65 | 1.48 | 1.37 | decaying, above bar |
| 2.0% | 1.51 | 1.41 | **1.36** | **flattening** |
| 2.5% | 1.46 | 1.37 | **1.35** | **flattening** |
| 3.0% | 1.47 | 1.41 | **1.39** | **flattening** |
| no stop / 2.5×ATR | 1.38 / 1.34 | 1.12 / 1.11 | 1.19 / 1.17 | below bar, stays dead |

That is what a real edge looks like under accumulating out-of-sample data: the in-sample
number decays toward a stable positive value instead of collapsing through it. Three
consecutive passes, every percentage-stop 2-day cell above the pre-committed 1.2 bar, the
loose-stop family dead all three times. The Gate-K-blocked cohort matured to **n=139** and
sits at PF 1.03–1.11 under the same policy — the gate's rejects are roughly breakeven at
2-day holds, which says the edge is thin-but-real and **the exit determines the sign**.

Also learning: the earnings calendar (born from WMT, self-refreshing, first autonomous
alarm cycle proven), the regime dataset (which surfaced the E4 question), the tail canary,
and Gate-K itself — which updates on every close.

**The LIVE loop is frozen by construction, and that is the honest problem.** Gate-K judges
only executed certified trades. Blocked longs never execute, so the live sample cannot
grow, so the PF cannot move except by quarantine rulings or the calendar (first loss ages
out **2026-10-18**). A_long has been pinned at n=27 for four sessions. The freeze is
epistemically self-sealing on the live side — all learning has migrated to shadow, which is
exactly what the shadow machinery was built for. The system is learning. It is not allowed
to act on what it has learned. **That conversion is a governance decision, not an
engineering task, and no amount of further analysis substitutes for it.**

## 3 · The way out — Conclave package, recommended THIS WEEK

Three paths exist. Waiting for October 18 is path three and I reject it as a default.
Path two alone (D2 quarantine + a green DGX close → PF ≈ 1.10) unlocks **the exact
configuration that produced 15 consecutive losers** — fastest, but it resumes the system
we measured to be broken. The right package combines the ruling with the fix:

1. **Adopt the E1 exit policy for longs: 2.0–2.5% stop + hard 2-day time exit.**
   Evidence: three OOS confirmations, robustness across every percentage stop, independent
   cross-validation (RCF scorer), C-cohort breakeven+, and the live book's own decomposition
   (zero mechanical profit-takes ever; WMT/SHW both earnings-window gaps now guarded).
2. **Rule D2** — quarantine WMT-08-20 per the ratified SHW doctrine (WMB's clean −0.997R
   the same morning is the control that proves the machinery; WMT was a gap).
3. **Re-baseline the certified cohort at the policy change.** The 0.6585 sample measured a
   system that no longer exists (different exits, no earnings guard). Gate-K already has
   the mechanism: a fresh cohort starts under the **probation path** — longs resume at
   probation size (0.5% risk, ~half normal), the gate rebuilds its judgment on the new
   system's own record, and it will re-freeze automatically if the new policy
   underperforms its shadow numbers.
4. **Nothing else changes.** Short halt stays. Earnings guard, kill-switches, caps, K3,
   regime gate — all untouched. This adds a discipline (time exits) and removes nothing.

That is the honest "beat Citadel" answer at this stage: the measurement infrastructure is
built and working — what separates a research desk from a trading desk is converting
converged evidence into sized positions under pre-committed rules. This package is that
conversion, at half size, with every brake still on.

**What I need from you: one word on convening.** The Conclave brief is effectively
written — it is E1-VERDICT + the three-run table above + this document. If you convene, I
will assemble the formal decision doc with the exact gate_config/lineage changes specced
for ratification, each with its Maya suite, before anything deploys.

## Ledger

- E1 series: 3 runs / 136 cells each in `quantum.e1_exit_grid_results`; next re-run only
  needed if the Conclave asks for a fourth confirmation.
- gov 240 view live: `quantum.v_qtp_why_no_trades` — repoint the scheduled check at it.
- DMS liveness leg: first scheduled run green (exec 644032).
- Watch: DGX (+$388, trail-managed); D2 math pends the ruling.
- Standing: shorts halted; two-week no-touch on gates (a Conclave ratification is the
  sanctioned amendment path, not a violation of it).
