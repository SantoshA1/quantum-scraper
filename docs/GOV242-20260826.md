# gov 242 — The Policy Invariant Monitor: issues get found by a machine at 18:25, not by losses at 10:27 (2026-08-26 afternoon)

**PO question answered by this build:** "we find new issues every day — what can we do?"
Every incident this week was one disease: **a ratified policy existed as prose while
its constants lived in 2–3 places in code** (the 1.2% stop regime: entry clamp + TSM
+ EX-C3). Reviews find copies when they know the literal; gov 241c proved a copy can
hide under a different literal (0.0115). The structural fix is not more review — it
is a machine that re-derives every ratified invariant from **broker and ledger
truth** nightly and alarms by name. That machine is now live. Alongside it: a
**mechanics freeze** — nothing in the entry/exit path changes until the probation
cohort delivers its verdict (n=20, or the pre-committed interim look at n=10 if
PF<0.6).

## 1 · QTP Policy Invariant Monitor (`1sDON3ev6zJo3pGR`, published `00dbd876`, nightly 18:25 ET)

Read-only. One Telegram line every weeknight — **a green heartbeat or named
violations; silence is impossible by design** (a check that cannot run reports
`MONITOR_BLIND_*`; the gov-235 lesson that a silent guard equals a dead guard).

| inv | verifies (ratifying gov) | source of truth |
|---|---|---|
| I1 | every long entered today carries a 2.35–2.65% stop; re-anchors legal only inside the band (241/241c) | ledger |
| I2 | every post-epoch open long: live stop coverage ≥ qty, width 2.3–2.7% of entry, TP ≥15% away (241c) | Alpaca orders, nested |
| I3 | protective legs are gtc (211) | Alpaca |
| I4 | `edge_baseline_epoch` exists AND equals the pinned 1787692697 — a 1-second rewrite alarms (241) | gate_config |
| I5 | probation risk ≤ 0.55% of live equity (241 D3) | ledger × /v2/account |
| I6 | no post-epoch long ever reaches session 3 — the 15:50 exit actually fired (241) | ledger × calendar |
| I7 | main pipeline / TSM / time-exit still published+active (limitation: public API exposes `active`, not the version pointer — I1/I2 geometry is the deep detector) | n8n API by $vars name |
| I8 | earnings calendar < 3d stale (235/239) | quantum |
| I9 | zero short entries past the halt (219) | ledger |
| I10 | cohort n≥10 with PF<0.6 → "interim look due" fires automatically (241 pre-commitment) | epoch views |

Suite `tests/test-pim-20260826.js` — **23/23 + 4 biting sabotages** (violation
collector muted, blindness silenced, epoch pin dropped, SQL epoch scope dropped),
executing the REAL node bytes against mock broker/n8n. PIM-05 is a witness: a
1.15% live stop — yesterday's incident geometry — fires `I2_STOP_DRIFT`.

**First light, live (execution 654438):** the smoke run correctly flagged all three
of this morning's defective entries. v1.1 (published) excludes PO-adjudicated
`RECERT_QUARANTINE%` rows — the quarantine IS the resolution record. **Tonight's
18:25 message will therefore show ONE known I1 line (FLEX's historic entry, already
remediated live at the broker) and reads green from tomorrow.** DGX is legacy
(pre-epoch) and correctly outside I2's policy geometry.

## 2 · H4c: the exit classifier learns the time exit (`bBIAbsClonHP94hk`, published `a58b9536`)

`classifyExitReason` now checks evidence first: `client_order_id` prefix
`qtp_timeexit_` → `exit_reason 'time'` (was falling through to `'manual'`,
mis-labeling the exact policy the cohort measures; the schema's CHECK already
allowed `'time'`). One 428-byte insertion, count-asserted; suite
`tests/test-h4c-timeexit-reason-20260826.js` **8/8 + sabotage**, with the witness
pinning old bytes → `'manual'` and regressions pinning stop/trail/target/manual
untouched. Chain: 498085c4 (v2, 08-04) → **a58b9536 (H4c)**. Friday's FLEX exit
(if it runs) lands correctly labeled.

## 3 · What "constantly execute profitable trades" actually requires now

Mechanics are, for the first time, **provably** what the Conclave ratified — placed
stop = kept stop = judged stop = 2.5%, hard 2-day exit armed, and a nightly machine
that catches drift the night it appears. What remains is not an engineering gap:
**profitability is now an edge question, and only the cohort can answer it.** The
policy running is the one E1 measured at PF 1.35–1.46 out-of-sample across three
runs. The system's job this week is to be boring: accrue probation entries at 0.5%,
let Gate-K scale or freeze on its own record, and let PIM prove every night that
the policy on paper is the policy in the market.

## 4 · Pending (the honest list)

1. **Tonight 18:25** — first scheduled PIM heartbeat (1 known FLEX line, then green).
2. **Friday 15:50** — FLEX 2-day time exit (session math: entered Wed → due Fri).
3. **Cohort accrual** — gate auto-judges at n=20; I10 automates the n=10 tripwire.
4. **October window roll** — legacy losses age out of the 90-day R2 window.
5. Deferred by the freeze (data-only, next Conclave's menu): E3b earnings retro-cost;
   shadow-kill profitability audit (BROAD_SCANNER_BIAS_PATH kills ~50/day — is the
   scanner's own bar leaving measured money on the table?); short-side re-entry
   study; the PO's 30-min checker prompt rewrite to gov-241 rubric (drafted on ask).

Freeze discipline: PIM and H4c were the last deploys. From here, code changes to the
entry/exit path require either a PIM violation or the cohort verdict.

## Addendum (2026-08-27 evening) — PO rulings on the external review's "not fixed" list

- **QTP_NAKED_FLATTEN stays OFF during probation** (PO-ratified 08-27). Verified from
  deployed TSM bytes: recovery ALWAYS re-protects a naked name (gtc stop at
  min(intended, current×0.995)) regardless of the flag; the flag only swaps that
  recovery stop for an immediate market flatten when price is already >0.5% past the
  intended stop — bps of difference, with gov-189-class false-positive risk on stale
  prices. The real exposure is the 15-min detection cadence, which the flag does not
  change; the real fix (event-driven protection off the Alpaca trade-updates stream)
  is kernel-class → Conclave menu.
- **Documented exposure (risk ledger):** time-exit worst case — protective-leg cancel
  succeeds, market close fails → name naked ≤ ~10 min (15:50 → next TSM cycle),
  loudly alarmed per-symbol. Accepted, bounded, monitored.
- **main fast-forward to 2346669 (merges PR #16) ratified**; blocked from the session
  bridge by missing push credentials — one step for any credentialed clone:
  `git push origin fix/tsm-bars-window-20260804:main` (verified 87 ahead / 0 behind,
  docs/tests/lib only).

## Correction (2026-08-27, PO-flagged) — the hot-path AI is Claude, not Grok

Verified from deployed bytes (05e46ef6/2886a1b2 lineage): "Grok AI Analysis" and
"Grok Signal Analyzer" call **api.anthropic.com** with `$vars.ANTHROPIC_API_KEY`,
model claude-opus-4-8; there are **zero api.x.ai calls anywhere in the pipeline**.
"QTP MTF AI Judge (Perplexity/Grok)" calls no external AI vendor at all, and
"Persist Grok Verdict" merely stamps the legacy `grok-sig-v4.4` label into Postgres.
The node NAMES predate the vendor migration and were never renamed. Both external
reviews — and this desk's own briefs — repeated "Grok on the hot path" from the node
names; the vendor-agnostic architectural point (an LLM API on the hot path,
fail-closed) stands, the vendor attribution was wrong. Rename blast radius measured:
zero `$('…')` expression references, wiring-keys only — a provable zero-parameter
cosmetic rename. The `grok-sig-v4.4` DB stamp stays as-is for analytics continuity
until a ratified bump.
