# Layer-by-layer pressure test, 2026-08-14 11:55 ET — zero entries is the gates working, and every gate was verified on live evidence

**For:** PO · **Question:** *"No new executions. Pressure test every layer and check why no new orders are executed."*

---

## The funnel today, with the layer that stopped each candidate

| layer | in → out | evidence |
|---|---|---|
| L0 scanner | firing every 5 min, all success | 42 signals ingested by 11:40 |
| L1 ingest | 42 signals → 22 distinct candidates | repeat suppression (ALB ×6-type dedup) |
| L2 state machine | 22 → 21 pass | ASTS passed too; zero SSM kills today |
| L3 pause guard | **open** — reader returns NOMINAL | 7 early candidates (09:30–09:35) were killed here **before your 09:37 lift**; everything after flows through |
| L4 filter stack | 21 → 0 survivors | REGIME_CONFLICT 10 · BROAD_SCANNER_BIAS 5 · AI_CONFLICT 2 · PAPER_SECONDARY 2 · BIAS_FILTER 1 · +1 late-path (ASTS, short → would meet Gate-K short block) |
| L5 caps/Kelly | untouched today — nothing reached it | caps 0/5 used; last exercised 08-12 |
| L6 orders | 0 entries · **1 exit executed** | ZBRA stopped out 11:03 ET, −$124.60, stop FILLED at 371.98 — the order path works, in the only direction asked of it today |
| L7 brakes | all armed, none tripped | KS monitor green every 2 min (cum $0 vs −2,500), day-P&L leg live, sentinel 2/2 OK |

## The pressure test — each layer proven on live bytes, not assumed

**The #1 killer today (REGIME_CONFLICT, 10 kills) is NOT another dead-input guard.** I pulled the
AMD 09:45 execution end-to-end: the Options Flow Engine returned live data — net GEX +$65.3M,
P/C 0.36 OI / 1.58 vol, IV skew 90.5%, three same-day put sweeps (8–20× OI) → regime
`CONTRARIAN_SHORT (VERY_HIGH)`; the Dark Pool Engine returned live data — short ratio 50.6%,
A/D HEAVY_DISTRIBUTION, institutional score −80 → `STRONG_DISTRIBUTION (HIGH)`. Both opposed
the BUY → `CONTRA_BOTH` → hard veto. The AI judge independently scored it HOLD, confidence 22
("low-quality long into distribution"). That AMD long would have been entered into exactly the
kind of institutional selling the filter exists to avoid. **This layer earned its kill today.**

**The pause layer is open both ways** — verified this morning live: WMB blocked at 09:30 under
the halt with the gov-213 reason; ALGN/AMD flowed past the guard at 09:45 after your lift and
died at the real filters instead. The 7 "PENDING" audit rows from 09:30–09:35 are those
pre-lift pause kills (their SKIP path never writes back a final status — known gap).

**The exit/order layer proved itself at 11:03**: ZBRA's GTC stop fired and filled — entry
plumbing and exit plumbing share the same order path, and it worked today under real
conditions. Book is now 5 open, all re-armed FULLY_PROTECTED this morning.

**Is zero-by-noon abnormal?** Last 21 trading days: avg 2.4 entries/day, **4 of 21 full days had
zero entries.** Half a day with zero, in a session where the deepest filters are killing on live
institutional-flow evidence, is inside normal operation.

## Two findings for the ledger

1. **SKIP-path audit write-back gap (observability, not trading):** kills on certain late SKIP
   paths (today: ASTS; the pre-lift pause kills; yesterday: ARM's ATR kill) leave their early
   audit row stuck at `PENDING`. The audit table under-states attribution. Fix is a write-back
   on the SKIP route — small, already on the list.
2. **Credential hygiene (MEDIUM):** the Indicator Enrichment node embeds the Polygon API key as
   a field (`_polygon_key`) in every signal item, so it rides through every downstream node and
   into n8n execution logs. **Verified NOT persisted to Supabase** (0 hits in `strategy_signals`
   and `audit_trail`), so exposure is n8n-execution-data only — but it should be removed from
   the payload and read from `$vars` at point of use. Needs your word; 15-minute fix.

## Bottom line

Nothing is stuck, paused, or broken. Signals flow, the pause is open, and every candidate today
was rejected by a **named filter operating on inputs verified live** — including the first
confirmation since gov 212 that the regime layer kills on real options/dark-pool data rather
than a dead field. The system is doing precisely what it is configured to do; whether those
filters *should* be the strategy remains the measured question it has been all week (gov 212:
the funnel's survivors historically did not beat its rejects).
