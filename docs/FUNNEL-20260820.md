# gov 236 — 08-20 funnel: yes, QTP is behaving exactly as designed — and that is now the problem

**14:32 ET · read-only.** Every one of today's **188 audit rows is accounted for**; zero
unexplained kills; every guard shipped this week fired correctly on live data. The system
is healthy. The *book* is not, and today's arithmetic closed the last self-service exit.

---

## 1 · Why 0 trades from ~159 VC passes (121 at your check, 159 by 14:32)

VC PASS means "the signal is coherent," not "we should trade it." Seven gates sit
downstream. Today's full ledger:

| kill | n | share | what it is |
|---|---|---|---|
| **SHORT_SIDE_HALT** | **74** | 39% | gov 219: shorts halted at PF 0.28 over 25 certified |
| BROAD_SCANNER_BIAS_PATH | 37 | 20% | bias score below the scanner's own bar |
| **GATE_K `negative_measured_edge`** | **19** | 10% | every long: BSY, BX, SMCI, WDC, AMT, DOW, PCG… |
| REGIME_CONFLICT_SHADOW | 17 | 9% | logged only, passed through (E1 data) |
| SSM_KILL | 12 | 6% | duplicates + extreme-ATR (ROST 14:05 was a same-day dupe) |
| BIAS_FILTER / PAPER_SECONDARY | 20 | 11% | volume_ratio ≤ 1.25 |
| AI_CONFLICT | 3 | 2% | analyst disagreed with the signal (CPT, SRE ×2) |
| ENTRY_PAUSE | 2 | 1% | 10:05 advisory during the WMT-stop reconcile lag; self-cleared |
| MTF / blocklist | 4 | 2% | — |

**Two gates account for half of everything: the short halt and the long edge-lock.** Both
are PO-ratified policy responding to measured losses. Nothing is stuck, mis-routed, or
silently failing.

## 2 · The guards shipped this week — all three proved themselves live today

- **gov 232 (auth):** `AUTH_FAILED = 0` today, second clean day. The gap-override class
  that died anonymously 3×/day is gone.
- **gov 231 (kill-switch):** stop leg counted **2 real fills** (WMT 09:34, WMB 11:44) —
  on the pre-fix bytes this would read **0 forever**. 2 < 4 → correctly no trip, zero
  false pause rows. The tripwire is alive and calibrated.
- **gov 235 (earnings):** calendar fresh (1.5h, 1,656 symbols) and **correctly silent** —
  I joined today's 19 Gate-K longs against the calendar: **none** is within 3 days of a
  print. The guard did nothing because there was nothing to do, which is the hardest
  behavior to verify and now verified.
- Cumulative kill switch: **−$1,287.64 of −$2,500 (51%)**. Pause reader NOMINAL all day.

## 3 · Today's three exits — and the one that vindicates the machinery

| exit | R | net | verdict |
|---|---|---|---|
| WMT 09:34 | **−7.93R** | −$933 | earnings gap-through — risk-model failure, not signal |
| XPEV 10:51 | +1.09R | +$438 | PO manual close ahead of Monday's print — clean |
| **WMB 11:44** | **−0.997R** | −$125 | **textbook stop: 1.178% wide, filled at −1.00R** |

**WMB is the control experiment.** Same stop machinery, same day, no gap: it capped the
loss at exactly the risk it was sized for. That is the proof that WMT's −7.93R was an
*overnight-gap* event, not broken execution — and it is the strongest argument yet for the
gov-222 quarantine doctrine applying to WMT.

## 4 · The finding that forces a decision: the unlock path closed today

Yesterday the long side was ~$54 of green from unlocking. After WMT (−$933) and WMB
(−$125), the certified long sample is **n=27, gross win $2,145.27, gross loss $3,258.05,
PF 0.6585**, and **DGX is the only open position left** (+$314 unrealized).

| scenario | resulting PF | unlocks? |
|---|---|---|
| today, as-is | 0.6585 | no |
| DGX closes green at its current mark | 0.7550 | **no** |
| WMT quarantined (gov-222 doctrine), DGX still open | 0.9228 | no |
| **WMT quarantined AND DGX closes at mark** | **1.0581** | **YES** |

**No combination of open positions can unlock the long side any more.** There is exactly
one path back to trading longs before old losses age out of the 90-day window (materially,
October): **the WMT quarantine ruling plus a green DGX close.** That is not me arguing for
the quarantine — it is the arithmetic of where the ruling now sits. The tail canary also
advanced to **15** consecutive certified-long losers.

## 5 · Direct answer to "is QTP behaving as expected?"

**Mechanically: yes, completely.** Ingestion, cursor, audit, gates, exits, reconcile,
kill-switch, auth, earnings calendar — all verified live today, nothing unexplained.

**Strategically: it is doing exactly what a correctly-built system does when its measured
edge is negative — refusing to trade.** The freeze is the feature working. But a system
that cannot trade also cannot generate the evidence that would unfreeze it. That loop is
now closed by arithmetic, and only three things can open it: (a) the quarantine ruling,
(b) October's window roll, (c) adopting a measured-better policy — which is precisely what
E1 produced yesterday (2-day time exit, PF > 1.2 on both cohorts at every stop width).

## Ledger — what I need from you, in priority order

1. **D2: the WMT quarantine ruling.** Now the only near-term unlock path. My
   recommendation stands: quarantine (WMB proved the stop machinery works; WMT was a gap),
   paired with the structural fix — which now exists and is live (gov 235).
2. **E1 adoption path** — fresh-data shadow re-run starts tomorrow (`e1_20260821`); two
   weeks of ≥1.2 PF on data that did not exist when the policy was chosen makes it a
   Conclave package.
3. Optional, no urgency: E3b retro-measurement (what holding through prints has cost),
   5 MCP toggles, gov 230 dashboard cleanup.

Standing: shorts halted, two-week no-touch on gates — nothing today argues otherwise.
