# gov 237 — 08-21: the regime lock lifted, Gate-K is now the only wall, and E1 survived its first out-of-sample test

**12:10 ET · read-only on the trading path.** Your pasted 30-min report is **confirmed on
every number I could recompute**, its one open caveat is **resolved**, and two things
changed today that the 10:08 check could not have seen.

---

## 1 · Your report, verified line by line

| claim | recomputed | verdict |
|---|---|---|
| tail canary: 15 consecutive certified longs since WSM (08-04) | **15** | ✓ exact |
| winner rate 5 of 28 (≥3.6%), baseline 4/20 → no drop | **5 of 28** | ✓ exact |
| r_multiple integrity: 0 violations | **0** over 7d | ✓ |
| no order events today (at 10:08 ET) | true then; **3 by 12:00** — XYZ, ADBE, CAG, all `STAND ASIDE / NO_POSITION` | ✓ (scalp-watcher no-ops, not orders) |
| DGX sole open position, live gtc stop | **+$422.78, FULLY_PROTECTED, 1 stop** | ✓ |
| no stale prior-day capped-entry orders | confirmed — 2 order events, neither an entry | ✓ |
| gov-210: alarm is review, not auto-halt | correct reading | ✓ |

**Caveat resolved:** `verify-gatek.sql` is *not* unreachable — it's on the Mac at
`docs/gatek-conclave-20260807/verify-gatek.sql` (10,905 B) and I read it through the device
bridge. It also isn't needed for the PF number: I compute the certified cohort from
**`compute_kelly_gate`'s own live function body**, which is the authority the SQL file
merely exercises. Both of your numbers are right and measure different things — the
report's **28** is the winner-rate universe (`side='buy'`, closed, unquarantined); Gate-K's
**27** additionally requires `r_multiple`, `risk_amount>0`, `intended_stop`, and a
90-day *entry* window. No discrepancy.

## 2 · What changed since your check: the regime lock lifted

`regime_state` today: **RISK_OFF (all day yesterday) → CHOP 10:30 → RISK_ON 11:30, LOW
vol.** That matters more than it sounds:

- **Yesterday longs were double-locked** — Gate-K *and* the counter-regime rule
  (`counter_regime_bullish_in_downtrend`).
- **Today only Gate-K blocks them.** The market turned supportive and the second lock
  opened on its own.

The funnel shows exactly that shift. 108 rows today:

| kill | today | yesterday | why it moved |
|---|---|---|---|
| **GATE_K** | **28 (#1)** | 19 (#3) | more longs now *reach* the gate — regime no longer kills them first |
| BROAD_SCANNER_BIAS_PATH | 24 | 37 | — |
| REGIME_CONFLICT_SHADOW | 18 | 17 | logged only |
| **SHORT_SIDE_HALT** | **13** | **74** | RISK_ON produces far fewer bearish signals |
| BIAS_FILTER / PAPER_SECONDARY | 12 | 20 | volume_ratio ≤ 1.25 |
| AI_CONFLICT / SSM / MTF / blocklist | 13 | 21 | — |

**Answer to "what is holding": Gate-K alone, at PF 0.6585 over n=27.** 28 longs today —
ORCL, UNH, TGT, VRTX, REGN, SCHW, ANET, ACN… — died there and nowhere else. Guards clean:
**0 AUTH_FAILED** (gov 232, day 3), **0 EARNINGS_WINDOW** (correctly — no candidate today
is within 3 days of a print), calendar fresh from its **first unattended nightly run**
(execution 620298, 18:10 ET, success).

## 3 · E1's first out-of-sample test — the finding survives, with a real cost

Ran the promised fresh-data re-run (`e1_20260821`, 136 cells, engine byte-identical, only
`RUN_ID` changed). Samples grew on their own: **A_long 21 → 27**, **B_blocked 324 → 395**.

| stop × time_2d | A_long 08-19 → 08-21 | B_blocked 08-19 → 08-21 | still clears? |
|---|---|---|---|
| 1.0% | 2.59 → **1.64** | 1.65 → **1.48** | ✓ |
| 2.0% | 2.62 → **1.73** | 1.51 → **1.41** | ✓ |
| **2.5% (proposed)** | 2.36 → **1.60** | 1.46 → **1.37** | **✓** |
| 3.0% | 2.18 → **1.52** | 1.47 → **1.41** | ✓ |
| 1.5×ATR | 2.06 → 1.52 | 1.37 → 1.21 | ✓ (thin) |
| 2.5×ATR | 2.29 → 1.63 | 1.33 → **1.11** | ✗ **failed** |
| no stop | 2.29 → 1.63 | 1.38 → **1.12** | ✗ **failed** |

**Read this honestly: every PF decayed.** Qualifying cells fell 13 → 8. That is what an
out-of-sample test is supposed to do to an in-sample number, and it is why the pre-committed
bar existed. Two things matter:

1. **The core claim held** — the 2-day time exit still clears PF > 1.2 on both cohorts at
   *every percentage stop width*, on 71 new blocked-BUY candidates and 6 newly-matured
   executed trades that did not exist when the policy was chosen.
2. **The failures are informative, not random**: the two cells that dropped out are the
   *loose* ones — no stop and 2.5×ATR. The tight-stop family survived. That **strengthens**
   the specific proposal (2.5% + 2-day) rather than weakening it, and it kills the "just
   remove the stop" variant on evidence.

One week of confirmation done, one to go. Next re-run Monday (`e1_20260825`).

## 4 · The decision that stopped being academic today

Book: DGX only, **+$422.78** unrealized (up from +$314 yesterday). Certified longs
unchanged at n=27 / PF 0.6585 — nothing closed today.

| scenario | PF | unlocks? |
|---|---|---|
| as-is | 0.6585 | no |
| DGX closes green at mark | 0.7882 | no |
| WMT quarantined only | 0.9228 | no |
| **WMT quarantined + DGX green at mark** | **1.1047** | **YES** |

Yesterday this ruling was academic — the regime blocked longs regardless. **Today it is
not.** RISK_ON + LOW vol is a supportive tape, 28 qualified longs are queuing daily, and
the quarantine + DGX combination now clears 1.0 with room (1.10, better than yesterday's
1.058 because DGX gained). If you rule, longs resume into a favorable regime; if you don't,
QTP watches this tape go by until October's window roll.

## Ledger

- **D2 (WMT quarantine) is the live decision.** Recommendation unchanged and now stronger:
  WMB's −0.997R clean stop on 08-20 proved the machinery works; WMT was a gap, and the
  structural fix (gov 235) is live so a third one can't happen silently.
- E1: 8/34 cells clear on fresh data; 2.5% + 2-day exit survives. Monday re-run next.
- Guards: 0 AUTH_FAILED (3 days), earnings calendar self-running, kill-switch calibrated.
- Standing: shorts halted, two-week no-touch on gates.
