# gov 241 — Conclave ratification EXECUTED: new exit policy live, long side reopened on probation (2026-08-25 evening)

**PO ratified Decisions 1, 2, 3 (Conclave doc `docs/CONCLAVE-20260825.md`, commit 7b26f56).**
Build order was safety-sequenced — mechanics first, activation last — and every stage
deployed tonight with the market closed. **Epoch: `1787692697` = 2026-08-25 17:18:17 ET.**
First eligible entry under the new policy: tomorrow 09:30.

## What is now live

| decision | implementation | state |
|---|---|---|
| D1 exit policy: 2.5% fixed stop + hard 2-day time exit (longs) | 3 node patches + 1 new workflow | **LIVE** |
| D2 quarantine WMT-08-20 | ledger row `eec1dd7e…` `RECERT_QUARANTINE_gov241_20260825` | **APPLIED** (loss NOT erased — cum. kill switch still counts it) |
| D3 re-baseline: fresh probation cohort judged on its own record | Gate-K v3.0 epoch clause + `edge_baseline_epoch` config row | **ACTIVE** |
| D4 (unchanged by design) | short halt, earnings guard, kill switches, K3, caps, regime | untouched, verified live post-activation |

## Deploy ledger (all sha256-pinned, byte-verified post-deploy)

**Main pipeline `vaqfCaELhOEWnkdo`: … → cb21316f (gov 235) → `05e46ef6-bba9-4247-a987-d8e57bfe3923` (gov 241, published).**
Scope-assert vs the 379178db manifest across all 150 nodes: changed = exactly
{Signal State Machine (gov 232), QET Kelly SQL Build (gov 235), **Alpaca Paper Trade**, **QET Gate-K Prep**}.
- Alpaca Paper Trade `df7d8036 → db529b7b`: **Patch A** — the 1.2% entry-stop clamp
  (`MAX_ENTRY_STOP_PCT`) replaced by `POLICY_STOP_PCT = 0.025` fixed stop;
  **Patch B** — bracket TP moved from 2–3×ATR (which would exit inside a 2-day hold the
  measured policy doesn't have) to a far non-binding 25% limit; OCO structure retained
  so every position still carries a protective stop leg.
- QET Gate-K Prep `aff5743c → b00761ec`: mirrors the same 2.5% stop so Gate-K judges
  the order that will actually exist (stop-parity doctrine, gov 194).

**TSM `vFnPjyx8srnzcYgV`: 07e5220e → `5607e03b-f335-44cb-84fa-d4c063dc51cd` (published).** Scope: exactly {Trail Stops}, `5f22eddd → e61ad289`.
- **T1:** `MAX_PROTECTIVE_STOP_PCT 0.012 → 0.026`. *Policy-killer #1, caught in recon:*
  at 1.2% tolerance the TSM's width check would have CANCELED every 2.5% stop the
  executor placed and forced 0.9% back on — silently repealing the ratified policy
  daily. 2.6% accepts the policy stop; the runaway-width guard survives above it.
- **T2:** tier-ladder tightening SUPPRESSED (`QTP_TIER_TRAIL_ENABLED = false`, logged
  each time it would have fired). Ratified policy: TSM monitors, recovers missing
  stops, but does not tighten. **Deviation ledger:** this freezes profit-tier
  trailing for ALL positions including legacy **DGX** (its current stop stays;
  scale-outs keyed on tier advance are frozen with it). Reversible by flipping the
  literal; revisit if E-series ever measures trailing as additive.

**NEW workflow "QTP Policy Time Exit" `OZx8Lh15zzo7jrJp` (published `3de7ae90-bcb4-457e-94a4-31dd2f8af580`).**
15:50 ET weekdays: PG selector → close node → Telegram. Payloads authored offline,
suite-proven, then SDK-assembled by subagent and byte-verified twice (sha
`4b1d6167…` SQL, `5929ffbb…` JS — both cmp-identical to fixtures).
- **Fail-safe #1 (epoch-required):** a missing `edge_baseline_epoch` row yields ZERO
  rows — proven live BOTH ways tonight: epoch absent → 0 due (DGX open); epoch set →
  still 0 due (DGX entry 08-05 predates epoch). Legacy positions can never be
  time-exited; the 2-day rule applies only to trades entered under the policy.
- **Fail-safe #2 (calendar lag):** sessions counted from `scorer_bars_daily`
  strictly-before-today +1; a stalled bar feed can only DELAY an exit, never fire early.
- **Fail-safe #3 (blast radius):** LIMIT 10 + alarm line at 10.
- Close mechanics: cancel protective legs first (verify + one retry; refusal →
  loud fail, NO close while shares reserved), then market sell tif=day,
  `client_order_id qtp_timeexit_<SYM>_<YYYYMMDD>`; per-symbol failures never throw
  (gov 235 alarm lesson); paper-endpoint guard is the only hard throw.
- **Known gaps (accepted, documented):** exits at 15:50 ET, 10 min before the close
  E1 measured (conservative); H5 heal may classify these exits `exit_reason='manual'`
  until the classifier learns the `qtp_timeexit_` prefix — the client_order_id is the
  audit trail either way; on an NYSE holiday the cron still fires and the order is
  rejected loudly (rare, self-announcing).

**Gate-K v3.0** (`GATE_K_v3.0_EPOCH_gov241_20260825`, migration `qtp_gov241_gate_v3_epoch_and_views`):
epoch clause appended to BOTH R2 direction-sample branches only — R1 short-block
sample and K3 cooldown read the full unwindowed history on purpose (a re-baseline
must never soften the short seal or cooldowns). Epoch-aware views:
`v_gatek_certified_metrics`, `v_gatek_tail_canary`, `v_qtp_why_no_trades` (+ PROBATION verdict branch).

## Suites (all committed)

- `tests/test-exit-policy-mechanics-20260825.js` — **10/10** + 4-way sabotage (all bite):
  executes the executor math region, the full Prep node, and the TSM ladder region
  from the REAL artifact bytes. Field lesson pinned in-file: executed-region
  harnesses inject the region's INPUTS, never its intermediates.
- `tests/test-timeexit-20260825.js` — **21/21** + 3-way sabotage: S2 (epoch fail-safe
  dropped) and S3 (failure alarm muted) bite cleanly; S1 (first cancel loop dropped)
  was HEALED by the retry ladder — the correct S1 (both loops dead) bites 5 ways and
  shows the node fails SAFE even then: it refused to close while legs were open
  rather than closing unprotected.

## Post-activation verification (all live, 17:20 ET)

| check | result |
|---|---|
| Gate probe, long 2.5% stop | **approved, probation:true, risk_pct 0.5, $500/100k**, n=0 of 20, v3.0 tag |
| Gate probe, short | refused (`counter_regime_bearish_in_uptrend`; halt + probation seal behind it) |
| Certified-metrics view | empty — fresh cohort |
| why_no_trades verdict | `PROBATION (gov-241): new-policy long cohort accruing — n=0 of 20…` |
| Time-exit due (epoch set) | 0 — DGX excluded |
| Tail canary | reset 0/0, threshold 14 |
| DGX | open, untouched, +unrealized; tier-trail frozen per deviation ledger |
| Quarantine arithmetic | post-quarantine legacy sample n=26 PF 0.9228 (function/view parity); cum. kill switch unchanged −$1,287.64 of −$2,500 |

## Rollback (single-switch, pre-agreed)

`DELETE FROM quantum.gate_config WHERE gate_id='GATE_K' AND constant_name='edge_baseline_epoch';`
→ gate instantly reverts to judging the full legacy sample (blocked at PF 0.9228) AND
the time-exit workflow goes inert the same second. Full mechanical rollback if ever
needed: restore `cb21316f` (main) + `07e5220e` (TSM) and unpublish `OZx8Lh15zzo7jrJp`.

## Pre-committed tripwires

- **Interim look:** if the probation cohort reaches n=10 with PF < 0.6, I bring it to
  the PO before n=20 — we do not ride a failing cohort to full sample size.
- Cohort judged at n=20 by the gate itself: PF ≥ threshold → full sizing; below →
  it stays at 0.5% or freezes, automatically. Nobody has to remember anything.
- All legacy brakes armed: cum. kill switch (WMT still counts), daily switch, K3,
  earnings guard (entry block + nightly advisory + DMS liveness), short halt, caps.

## Watch tomorrow

1. First post-epoch entries: audit rows where GATE_K approves at `risk_pct 0.5` with
   `stop_width_pct 2.5`; orders carry ~2.5% stops and far TPs.
2. TSM logs: `[TSM gov241] … tier advance SUPPRESSED` lines = freeze working;
   any `width>2.6%` cancel would be an anomaly worth a look.
3. 15:50 ET: time-exit fires only if a post-epoch position is on session ≥2 —
   earliest possible real action is Thursday 08-27 (an entry tomorrow → due Thu).
4. Nightly 18:10 earnings advisory: any new position within 3 days of a print.
