# Funnel trace 2026-08-19 — zero orders, and every one of them is accounted for

**As of 10:55 ET · read-only** (n8n MCP disconnected this session — every deploy-side item
below waits for reconnection; nothing needed one today)
**Verdict up front:** QTP is entry-frozen **by its own risk governance, on correct data** —
longs are edge-locked at Gate-K, shorts are halted by gov 219, six symbols sit in K3 cooldown.
The pasted 14:05Z health check is **independently confirmed** on every number I could
recompute, and it **missed one real defect**: the consecutive-stop-out kill switch met its
written threshold yesterday and did not fire.

---

## 1 · Why zero orders: every VC-passed candidate, traced

51 candidates passed the VC gate between 09:30 and 10:50. Not one is unaccounted for:

| kill | n | what it means |
|---|---|---|
| **GATE_K `negative_measured_edge`** | **16 longs** | every long that cleared the bias filter — NWS, NWSA, ROP, SOLV, UNP, TMO, WAT, WY, AAPL, ADSK, ALLE, AMGN, AOS, ARES, AXON, BAX, BDX… |
| PAUSE_GUARD (gov-219 short halt) | 19 shorts | NXPI, NVR, RELY, OLED, SNA, STLD, TJX, TDY, WYNN, AFL, AKAM, AMD, AMAT, ANET, APLD, ASML… |
| BIAS_FILTER / PAPER_SECONDARY | 13 longs | volume_ratio ≤ 1.25 — incl. SRE, ZTS, AFRM, AVY at bias 100 |
| REGIME_CONFLICT_SHADOW | 18 longs | logged and passed through (shadow data accruing) |
| in flight at query time | AWK | PENDING, will be swept by the gov-217 finalizer |

**Nothing reached the Risk gate because Gate-K sits in front of it and is fail-closed.** The
answer to "why did VC-passed orders not make it to the risk gate and fill" is one sentence:
*the certified long book crossed under dollar-PF 1.0 on Tuesday's closes, and the gate is
doing exactly what it was built to do about that.*

## 2 · The crossing, verified independently

Recomputed with the exact live filter (quarantine exclusion included), not taken from the
pasted report: **n=25, gross win $2,145.27, gross loss $2,199.23, dollar PF 0.9755** — under
the 1.0 bar by **$53.96**. The five closes that did it, all stops, all confirmed in the
ledger: ZBRA 08-14 (−$124.60), ADPT 08-17 (−$100.85), AES* 08-18 (−$87.72, short book),
DDOG 08-18 (−$104.12), **DUK 08-18 14:01 (−$114.24, −1.05R)**, **DASH 08-18 14:15
(−$134.88, −1.35R)**. Open book is 4: WMT, WMB, DGX long + XPEV short — matches the report.

Q5 as written (cleaned long PF < 1.0 over ≥15 certified) is **met**. Per gov 210 that is a
recommendation to halt the long side — and mechanically the halt is **already in force**:
n=25 ≥ 20 means no probation path exists; every bullish signal returns
`negative_measured_edge`. A formal halt flag would be redundant with the gate. My
recommendation: **no additional action; the gate is the halt.**

## 3 · "Are we missing any good trades?" — the honest answer is no, and here is the evidence

Three independent measurements now point the same direction:

1. **The honest universe's own fills went 0-for-4.** ADPT, DDOG, DUK, DASH — the only entries
   the diverse universe has produced — all stopped out within ~1 session, −$454 combined,
   with *clean* stop executions (the mechanics worked; the picks lost).
2. **13 consecutive certified-long losers** since WSM on 08-04. The tail canary freezes at 13
   because no new long can open; only the three open longs can move it now.
3. **The blocked-BUY counterfactual is negative too.** The RCF shadow scorer's verdict at the
   real ~6-day hold with the live 1% stop: **PF 0.70, 14.4% win** over n=334. The population
   QTP's filters reject loses money the same way the population it accepts does.

The gate is not hiding winners from you. The entry strategy — 5-minute momentum plus
volume-ratio confirmation — is producing losers on both sides of every gate at current market
character. That is what "no edge, correctly measured" looks like, and it is precisely what
the 08-17 forecast said a safe-but-edgeless configuration would do.

**Proposal (build when n8n reconnects):** extend the shadow-scorer machinery to ingest
`GATE_K / negative_measured_edge` blocks exactly like RCF blocks. Then "are we missing good
trades" stops being a daily question and becomes a table with a PF on it.

## 4 · What the pasted health check missed: a dead tripwire

`gate_config` says `killswitch_consec_stopouts = 4`. Yesterday produced **four stop-outs in
one session** (AES 09:32, DDOG 10:10, DUK 14:01, DASH 14:15) and **six consecutive
stop-type losing exits** counting back through ADPT and ZBRA — either reading meets the
threshold. `entry_pause_control` contains **no kill-switch row, ever** (zero non-AFTO rows
since 08-17; the only writer that has ever fired is the PO's manual gov-213 halt). The day-P&L
legs legitimately didn't trip (−$441 vs −$500; −0.42% vs −2.5%) — but the consecutive-stop-out
leg **met its number and stayed silent**.

Root-cause requires the Expansion Kill-Switch Monitor's bytes (`awDk3AQesvO3SpQs`) — likely a
cohort-scoping filter (`sizing_meta`/lineage) that today's trades don't carry, the same
guard-can't-fire class as gov 216. **Blocked on the n8n MCP reconnect; top of the list when
it's back.** Exposure today is nil — Gate-K and gov 219 have everything closed regardless —
but this switch must work before it is ever the last line of defense.

## 5 · Morning verification of this week's deploys — all green

- **gov 227b** (key off the payload): first live morning ran clean — 51 VC verdicts, bias
  scores, Gate-K metrics and enrichment fields flowing all session, no SE-C1, options data
  present. The rotated key is working and no longer rides items into logs.
- **gov 224** (cursor): resumed at **N** exactly where Tuesday's close parked it, swept
  N→Z and wrapped into A–B — 64 distinct symbols by 10:55, letters `ABNORSTUVWXYZ`.
- **gov 226** (cancel ≠ execution): `fills_today = 0` and means it — no cancel or skip is
  wearing EXECUTED.
- **gov 225a** (reconcile): K3 list is live and accurate — DASH(99h), DUK(99h), DDOG(95h),
  AES(95h), ADPT(76h), ZBRA(<1h remaining).
- Regime `CHOP/NORMAL` — not blocking; Gate-K alone is the long-side wall today.

## 6 · How the long side unlocks

Gross win must exceed gross loss again. Three paths: **(a)** the three open longs (WMT, WMB,
DGX) close green — roughly **+$54 of combined realized gain** flips PF back over 1.0;
**(b)** old losers age out of the 90-day window — slow, starts materially in October;
**(c)** a PO ruling (quarantine or rule change) — nothing on the table qualifies; every loss
in the sample is a legitimate signal outcome this time. The three open longs are carrying the
book's future. A losing close among them deepens the lock *and* trips the 14-loser tail alarm.

## Ledger

- **P0 when n8n reconnects:** read the kill-switch monitor, prove why 4-consecutive-stop-outs
  didn't fire, fix with the usual discipline (suite + witness).
- Build: Gate-K blocked-BUY shadow scoring (extends the existing scorer; advisory only).
- Watch: WMT / WMB / DGX closes; the 14th-loser tail alarm; sentinel distinct-symbol floor
  still pending.
- Standing: two-week no-touch on gates holds — nothing in today's evidence argues for
  loosening anything; it argues the opposite.

---

## MIDDAY ADDENDUM — 12:31 ET re-verification (PO: "zero trade executions so far")

Re-derived everything live rather than trusting the 10:55 snapshot. **Same verdict, stronger
evidence, two new findings.**

**The freeze holds for the same reason, at 3× the sample.** 169 audit rows through 12:30
(vs 51 this morning), every one accounted for: GATE_K 33 long kills (16 new since 10:55 —
BKNG, BMY, DIS, F, FDX, GM…), SHORT_SIDE_HALT 35 (18 new), PAPER_SECONDARY volume-ratio 22,
BROAD_SCANNER_BIAS_PATH 15, MTF 6, BIAS_FILTER 5, ticker-blocklist 3, in-flight 2 (GRMN, GS
at 12:30 — normal). REGIME_CONFLICT_SHADOW logged 42 observations for the scorer (24 new).
Morning's in-flight AWK swept correctly (BIAS_FILTER 10:45). Cursor wrapped into B→G;
pause NOMINAL every 15 min; pipeline writing through 12:30. **Zero fills, zero exits** — so
the tail canary stays at 13 and the Gate-K sample is untouched: recomputed against the live
function's own predicate (strategy/mode/certification conditions included, byte-exact):
**n=25, gw $2,145.27, gl $2,199.23, PF 0.9755.** My first loose recompute said n=72/PF 0.48 —
wrong filter (pulled in `maya-negative-edge` test rows); corrected against
`compute_kelly_gate`'s own WHERE clause.

**The unlock is in the money, unrealized.** At 12:30: DGX **+$236.03**, WMT +$17.70,
WMB −$29.68 (combined **+$224.05** vs the ~+$54 realized needed; XPEV short +$347.49
besides). DGX alone, if it closed at its current mark, flips PF to **1.0828** and unlocks
longs. But only realized exits count, and exits are system-driven — if the gain evaporates
before a stop/trail/target triggers, the unlock recedes. (A manual close is a PO-only
discretionary override; on record as a lever, with the obvious tradeoff, not a
recommendation.)

**New finding 1 — unattributed AUTH_FAILED probes, started 08-17.** SSM kills with
`sm_action=AUTH_FAILED`, symbol UNKNOWN: 3 on 08-17, 6 on 08-18, 3 today (10:15, 11:05,
11:45). Not my gov 227 deploy (predates it; and the v2 seeder bytes provably preserve
sibling `_credentials` keys). Not a rotted internal caller — authenticated traffic is
flowing (CRDO reached the SSM and died on a *real* kill: ATR 8.7% > 8% untradeable,
`sm_action=PASS`). So either a caller added ~08-17 lacks the secret, or something external
found the public webhook path and the SM-C4 gate is correctly refusing it, fail-closed,
three times a day. No signal loss; identify the sender from execution logs on n8n
reconnect.

**New finding 2 — blocklist rows carry empty `blocked_stage`.** The SKIP-fast-path builder
(v1.1, 2026-07-20) writes `kill_stage_attribution='TICKER_BLOCKLIST'` but leaves
`blocked_stage` blank (XYZ 10:05, EOG 11:55, EW 12:00). Attribution is intact, so nothing
is uncounted — cosmetic labeling gap, queued behind the deploy backlog.

**Ledger delta:** reconnect queue is now: P0 kill-switch tripwire RCA → AUTH_FAILED sender
ID → Gate-K blocked-BUY shadow build → blocklist label fix → decommission toggles (Script
Scraper v6, Dead Man's Switch Railway leg).
