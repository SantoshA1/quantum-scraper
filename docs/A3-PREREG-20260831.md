# A′-3 pre-registration — scoring the retired AI research thesis (2026-08-31, BEFORE SCORING)

`quantum.daily_research_thesis`: 40 pre-market (08:30 ET) SPY calls, 2026-07-06 →
08-31, free-text `spy_view`. **Power statement, frozen up front: n=40 admits NO
graduation verdict** (a 55%-skill caller is indistinguishable from luck at this
n). This is a descriptive scorecard that closes the loop on already-paid-for
data. It cannot promote anything.

## Frozen classification rule (applied mechanically, case-insensitive)

- BULLISH if `spy_view` matches: `grind (modestly )?higher | upside bias |
  open(ing)? (mildly |modestly |with .*upside.* )?higher | positive opening
  momentum | follow-through buying`
- BEARISH if it matches: `downside pressure | open(ing)? lower | lower close |
  opening weakness | test(ed|ing)? lower | broad equity weakness | de-risking`
- Both or neither → NEUTRAL (excluded from scoring).

## Frozen scoring

A thesis is a SAME-SESSION call (pre-market, "today"). Score against SPY's
same-date close-to-close return from `scorer_bars_daily`: BULLISH hits if ret>0,
BEARISH hits if ret<0. Report hit rate, n scored, two-sided binomial p vs 50%,
and mean same-day SPY return conditioned on each label. No cost model, no
graduation — descriptive only.
