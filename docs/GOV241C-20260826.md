# gov 241c — Day-one incident: EX-C3 silently repealed the ratified stop; found, fixed, quarantined (2026-08-26)

**The first probation session ran the wrong policy.** All three entries (CHTR 09:41,
EQT 09:56, FLEX 10:01) placed the ratified 2.5% stop correctly — and then the
executor's EX-C3 fill-anchor block re-tightened every one of them to ~1.145% of fill,
the exact tight-stop family E1 rejected and the Conclave retired. CHTR (−1.03R,
−$132.66) and EQT (−1.18R, −$122.54) stopped out **at prices above their untouched
2.5% policy stops** (CHTR exit 153.01 vs policy stop 151.26; EQT 55.3212 vs 54.50).
Detection credit: the PO's scheduled 30-min checker reported the stop widths
faithfully — its verdict logic just predates gov 241 (see §6).

## 1 · RCA — the third private copy of a retired policy

| layer | constant | gov 241 status on 08-25 |
|---|---|---|
| executor entry math | `MAX_ENTRY_STOP_PCT 0.012` | **patched** (Patch A → POLICY_STOP_PCT 0.025) |
| TSM width tolerance | `MAX_PROTECTIVE_STOP_PCT 0.012` | **patched** (T1 → 0.026) |
| **executor EX-C3 fill re-anchor** | **`_exStopTargetPct 0.0115` / `_exStopTsmBar 0.0119`** | **MISSED** |

EX-C3 (APT v4.9, born from WST 08-10) re-anchors any stop whose realised width vs
fill exceeds its bar. Under the old regime that bar (1.19%) only caught pathological
fills; under the new 2.5% stops it caught **every** entry, and its target (1.15%)
re-imposed the retired policy on each one — `sizing_meta.stop_reanchored: true` on
all three fills is the smoking gun (`stop_price_initial` 151.26/54.50/108.89 = the
policy stops that were placed first).

**Why recon missed it:** the 08-25 sweep grepped for `0.012` and the two named
constants; EX-C3 encodes the same policy as *different literals* (0.0115/0.0119).
And the mechanics suite's executed region deliberately ended at the EX-C1 marker —
EX-C3 sits below it. **Field lesson (ratcheted into both suites):** a policy
constant is not changed until every consumer of its VALUE is found — grep the
semantics, not the literal you know; and the executed region must extend to every
block that can rewrite the value after placement.

## 2 · Patch C (live 2026-08-26 ~13:0x ET, mid-session)

`docs/policy-20260825/alpaca-paper-trade-patched2.js` (sha `30225d01…`, from
`db529b7b…`): `_exStopTargetPct 0.0115 → 0.025`, `_exStopTsmBar 0.0119 → 0.0259`,
plus the stale justification comment replaced by a **coupling contract**:
`2.5% target < 2.59% bar < 2.6% TSM tolerance` — keep the chain ordered or the
policy silently repeals. Count-asserted patch (`patch-policy3.py`, occurrence census
3+3, no-collateral proof, old literals extinct). Normal fills now realise
≈2.5%±slip ≤ 2.59% → EX-C3 never fires; pathological fills re-anchor to **2.5% of
fill** — the E1-measured geometry — never to a tighter family.

Deploy: subagent draft `2886a1b2-7e57-4e79-b371-c8995ae0c71f`, before-gate sha
matched, post-deploy byte-identical, my independent manifest scope-assert = exactly
{Alpaca Paper Trade} of 150, published. Chain: … → 05e46ef6 (gov 241) → **2886a1b2
(gov 241c)**.

## 3 · Suite: tests/test-exc3-policy-20260826.js — 9/9 + 2 sabotage

Executes the REAL defs region (constants + `_exFillStop`) and the REAL EX-C3
decision block. **EXD-03 is the regression star:** clean fill + policy stop → EX-C3
must NOT fire (yesterday's bytes fail it). **EXD-05 is the incident witness:** the
old constants under the same inputs reproduce ~1.15% — today's bug, executable.
Sabotage: reverting either constant bites (3 and 4 failures). Yesterday's entry
suite re-run on the candidate: only its sha-pin check moves (each artifact keeps its
own suite); noted caveat — its EP-10 scope check did not flag the new diff region,
so node-level manifest asserts carry scope duty (they did).

## 4 · PO rulings executed (ratified in-session)

**Quarantine CHTR + EQT** — `RECERT_QUARANTINE_gov241c_20260826` on rows
`496089ab…` / `3904d308…`, full rationale in lineage_source: execution defect, not a
signal or policy outcome; both exits above untouched policy stops; **the −$255.20 is
NOT erased** — it counts on the cumulative and daily kill switches (quarantine
removes rows from the edge sample only; verified the realized sum still includes
them). Post-quarantine: certified cohort n=0, canary 0/0 — the epoch now measures
only trades that actually run the policy.

**Widen FLEX to policy** — one-shot workflow `NPuV7YKBA9K1OHXv` (archived after
use). Three-probe saga, each abort proving the guards: probe 1 (652883) flat
open-query sees no nested children; probe 2 (652906) open+nested still empty with a
filled parent; v3 mirrors the TSM's `status=all&nested=true` and succeeded (652942):
stop 110.42 → **108.91 = 2.5% of fill**, atomic PATCH, no naked window. Verified by
re-running the one-shot: its own drift-guard read back "stop leg at 108.91" (652947).
TSM at 2.6% tolerance accepts it; ladder frozen; time exit due Friday 15:50 ET.

## 5 · State after gov 241c

| item | state |
|---|---|
| Executor | 2886a1b2 — placed stop = kept stop = 2.5% |
| Probation cohort | n=0 clean; next entry is datapoint #1 of the true policy |
| Today realized | −$255.20 (quarantined from edge sample; counts on switches) |
| Cum. kill switch | ~−$1,543 of −$2,500 (62%) by ledger arithmetic |
| Open book | FLEX long 93 @ 111.70, stop 108.91 (policy), protected; DGX legacy +unrealized, untouched |
| Shorts / guards | halt sealed; earnings guard quiet (no CHTR/EQT/FLEX/DGX prints in 14d); K3 quiet |

## 6 · Reconciling the PO's scheduled checker

Its facts were right; its rubric is pre-gov-241. "All inside the 1.2% stop cap — no
P0" now inverts: **compliance with the retired 1.2% cap IS the P0.** Its tail-canary
item (pooled n=29, drought 15, raw PF 0.58, "did Q5 fire?") is superseded by the
ratified re-baseline: the epoch-scoped canonical views (`v_gatek_certified_metrics`,
`v_gatek_tail_canary`, `v_qtp_why_no_trades`) answer it in one query — legacy-sample
Q5 action is moot; the Conclave's answer to that sample WAS the policy change +
probation. The checker's prompt should be updated to: stops ≈2.5% expected
(`stop_reanchored` true = alarm unless realised>2.59% pathology), probation risk
0.5% (~$100–260/trade under concentration caps), and the three epoch views as the
canonical health read. Happy to draft the replacement prompt text on request.

## 7 · Watch

1. Next entries: `sizing_meta.stop_reanchored` must be false (or true only with a
   >2.59% pathological fill, re-anchored to 2.5%-of-fill).
2. FLEX: TSM cycles must keep FULLY_PROTECTED at 108.91; time exit Friday 15:50.
3. Interim tripwire unchanged: cohort n=10 PF<0.6 → PO before n=20.
4. H5 heal classification of future `qtp_timeexit_` exits (known gap, gov 241 record).
