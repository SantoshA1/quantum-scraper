# QTP Batch 2026-07-10c — Secret Rotation (scanner remediation)

Responds to the GitHub secret-scanning alert on batch 2026-07-10b: the internal QTP
proxy token (48-hex) was committed inside 5 files by design, flagged by the scanner,
and has now been ROTATED OUT of code entirely.

## What changed

- Token moved to n8n instance Variable `QTP_PROXY_TOKEN` (value never in code, repo,
  or chat). All four call/validate sites now read `$vars.QTP_PROXY_TOKEN`.
- FAIL-CLOSED semantics: if the variable is unset, both proxies reject every request
  (`QTP_PROXY_TOKEN_NOT_CONFIGURED`) rather than accept anything.
- Files replaced in this batch (supersede the 10b copies):
  - `trailing-stop-manager-v2.2.json` (workflow version a6e5a529)
  - `qtp-alpaca-paper-broker-proxy.json` v1.1 (9c4e5ca5)
  - `qtp-telegram-proxy.json` v1.1 (7c86f54b)
  - `docs/trail-stops-v2.js`, `docs/alpaca-fill-activity-monitor-v2.js` (v2.2 sources)

## Verification (2026-07-10 ~16:41 ET)

- TSM production run 374985: positions/orders read through the proxy with the NEW
  variable token — round trip proven, audit SUCCEEDED, market-closed guards correct.
- Old leaked token replayed against the proxy (execution 374988): REJECTED
  (`QTP_PROXY_FORBIDDEN`). The value in git history is dead.
- All 5 staged files re-scanned: zero occurrences of the old token, zero 48-hex
  strings, zero Alpaca keys / Telegram bot tokens / GitHub PATs.

## Git history note

Commit 8bf3c14 (batch 10b) still contains the OLD token in history. It is
invalidated — replay-tested and rejected — so history rewrite is optional. If you
want the string physically gone: `git filter-repo --replace-text` with the old
value, then force-push (coordinate before doing this; it rewrites all SHAs).

## Secret inventory after this batch

No secret values exist in any workflow code or in this repo. Alpaca -> Alpaca-PAPER
credential; Telegram -> Telegram credential; GitHub -> GitHub-QET credential;
DB -> Postgres credential; internal proxy auth -> n8n Variable QTP_PROXY_TOKEN.
